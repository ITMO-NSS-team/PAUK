node_modules/
dist/
*.local

# Настоящий снепшот графа (пишет new_generate/generate_data.py --out-dir
# new_gui/private/data) — не исходники, и authors-detail.json несёт
# персональные данные. "private" — Vite-каталог статики (см. publicDir в
# vite.config.ts), а не входная точка сама по себе.
private/
