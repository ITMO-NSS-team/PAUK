// Настройки отрисовки — единственное место, где живут "магические числа"
// вида радиусов/ширин/отступов/зумов. Смена значения здесь меняет
// поведение сразу везде, где оно используется, а не в одном месте из
// нескольких, где оно случайно продублировано. Фронтенд-аналог
// pauk/gui/config.py на Python-стороне (тот же принцип: тюнинг-константы
// отдельно от логики, которая их использует).

export const MAP_CONFIG = {
  // Камера. minZoom/maxZoom ограничивают, насколько можно отдалиться/
  // приблизиться; initialZoom и fitPadding — только для самого первого
  // кадра при загрузке (map.fitBounds в app/main.ts), дальше пользователь
  // управляет зумом сам.
  initialZoom: 5,
  minZoom: 4.3,
  maxZoom: 15,
  fitPadding: 40,
  // Фон стиля карты (map/build.ts не трогает — это на уровне самого MapLibre.Map в app/main.ts).
  backgroundColor: "#ffffff",

  node: {
    radius: 4,
    radiusSelected: 8,
    strokeWidth: 1,
    strokeWidthSelected: 2,
    strokeColor: "#ffffff",
    // Цвет узла, у департамента которого почему-то нет своего color —
    // не должно происходить на реальных данных, но лучше serый фолбэк,
    // чем упавшее приложение.
    fallbackColor: "#9d9d9d",
  },

  edge: {
    color: "#9d9d9d",
    width: 0.6,
    opacity: 0.5,
    // Невидимый слой-приёмник кликов (EDGE_HIT_LAYER_ID в map/build.ts) —
    // шире видимой линии специально, чтобы по тонкому ребру было реально
    // попасть курсором.
    hitWidth: 14,
  },
} as const;

// Границы для UI-регуляторов фильтров (features/filters.ts) — сами текущие
// значения порогов живут в AppState.filters (core/state.ts), здесь только
// диапазон, в котором пользователь может их двигать. Совпадает по духу со
// старым GUI (там были те же пороги — max 30 для соавторства, max 15 для
// общих авторов публикаций, 2020-2026 для года), без слепого копирования
// более старых, ничем не обоснованных частностей той реализации.
export const FILTER_CONFIG = {
  coauth: { min: 1, max: 30 },
  sharedAuthors: { min: 1, max: 15 },
  year: { min: 2020, max: 2026 },
} as const;
