"""Настроечные константы для new_generate/generate_data.py и new_generate/layout.py —
ручки, которые реально различаются между author/pub/repo точками вызова.
Простые константы, без переопределения через переменные окружения — никто
не крутит число итераций ForceAtlas2 через env, проще поправить число тут.

Константы, нужные ровно одной функции без вариации между вызовами
(например, координатное пространство фронтенда, сигма разброса при
подмешивании) — локальные, рядом с этой функцией в layout.py, а не здесь.
"""

# --- Заглушки для отображения ----------------------------------------------------
NO_DEPT_NAME = "Без департамента"
NO_DEPT_NAME_EN = "No department"
NO_DEPT_COLOR = "#8a8f98"

# --- Пороги рёбер ------------------------------------------------------------------
COAUTH_MIN_W = 2  # мин. число совместных публикаций для ребра автор-автор
PUB_EDGE_MIN_W = 3  # мин. число общих ИТМО-авторов для ребра публикация-публикация
PUB_LAYOUT_TOP_K = 6  # сколько сильнейших "общий автор"-рёбер оставлять на публикацию для раскладки

# --- Число итераций ForceAtlas2, по одному прогону на тип сущности -----------------
FA2_ITER_AUTHORS = 300
FA2_ITER_PUBS = 250
FA2_ITER_REPOS = 100

# --- Синтетические рёбра "тот же департамент" --------------------------------------
DEPT_EDGE_K = 3  # со сколькими случайными коллегами по департаменту связан каждый узел
DEPT_EDGE_WEIGHT = 1.0  # сопоставимо с реальными рёбрами (совместные публикации начинаются с 1.0)
PUB_DEPT_EDGE_K = 1
PUB_DEPT_EDGE_WEIGHT = 0.5

# --- Минимальное расстояние между узлами (проход после раскладки) -----------------
MIN_SEP_AUTHORS = 4.5
MIN_SEP_PUBS = 3.5
