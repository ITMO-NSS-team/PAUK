"""Manual administration of the graph.

Today: the `pauk admin` commands. Later: the FastAPI panel, which will
call the same `pauk.graph.mutations` layer these commands do — the rules
about what a valid edit is live there, not in either front end.
"""
