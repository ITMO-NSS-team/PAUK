"""Generates data for the site: cache snapshot -> layout -> JSON. Entry
point and orchestration - `builder.py`, one module per stage
(`authorship.py`, `departments.py`, `layout.py`, `nodes.py`, `edges.py`).
Run as `pauk gui build` (`builder.py::write_site_data`).
"""
