"""pauk.gui - the site: `graph_builder.py` turns a `pauk.cache` snapshot into
layout + JSON (a lightweight summary for the map plus per-entity-type detail
files, see its docstring); `web/` is the TypeScript frontend that renders them.
"""
